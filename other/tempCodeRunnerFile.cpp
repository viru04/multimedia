bool flag = true;

        // for (set<string>::iterator it = cur.begin(); it != cur.end(); it++)
        // {
        //     vector<string> arr = wordsof(*it);

        //     if (freq[*it] < minfre)
        //         flag = false;
        // }
        // if (!flag) {
        //  break; // Stop the process if any itemset was infrequent
        // }